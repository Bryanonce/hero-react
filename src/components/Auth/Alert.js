import React from 'react';
//import Styles
import './Alert.css';

export const Alert = ()=>{
    return (
        <div id="alerta" className="alert alert-danger">
            <p>Credenciales Incorrectas</p>
        </div>
    );
}